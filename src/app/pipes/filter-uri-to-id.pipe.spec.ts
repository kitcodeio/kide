import { FilterUriToIdPipe } from './filter-uri-to-id.pipe';

describe('FilterUriToIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUriToIdPipe();
    expect(pipe).toBeTruthy();
  });
});
